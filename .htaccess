# Prefer login.html as the default DirectoryIndex for this site
# Place this file in the site root (same directory as index.html)

# Use login.html before index.html
DirectoryIndex login.html index.html

# Optional: redirect requests explicitly asking for index.html to the site root
# This avoids exposing /index.html while still serving the desired default page.
<IfModule mod_rewrite.c>
  RewriteEngine On
  # Redirect direct requests to /index.html to /
  RewriteCond %{THE_REQUEST} /index\.html [NC]
  RewriteRule ^index\.html$ / [R=301,L]
</IfModule>

# Security recommendations (optional):
# Disable directory listing
Options -Indexes

# End of .htaccess
